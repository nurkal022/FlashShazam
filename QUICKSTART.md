# Быстрый старт 🚀

## Установка

1. **Создайте виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate  # на Windows: venv\Scripts\activate
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

**Важно:** Для macOS может потребоваться установка PortAudio:
```bash
brew install portaudio
```

3. **Проверьте .env файл:**
Убедитесь что файл `.env` существует и содержит ваши API ключи.

## Запуск

```bash
python app.py
```

Откройте браузер: http://localhost:5001

**Примечание:** На macOS порт 5000 часто занят AirPlay. Приложение использует порт 5001.

## Использование

1. Нажмите большую кнопку "🎤 Нажми для записи"
2. Разрешите доступ к микрофону в браузере
3. Воспроизведите музыку или поднесите устройство к источнику звука
4. Подождите ~15 секунд записи
5. Система автоматически:
   - Распознает трек через Shazam
   - Найдет метаданные через Spotify Scraper
   - Скачает MP3 с YouTube
   - Покажет результат с плеером

## Структура файлов

- `recordings/` - временные WAV файлы записей
- `downloads/` - скачанные MP3 файлы с метаданными

## Устранение проблем

**Ошибка с микрофоном:**
- Проверьте разрешения браузера на доступ к микрофону
- Убедитесь что PortAudio установлен (macOS/Linux)

**Ошибка распознавания:**
- Убедитесь что музыка играет достаточно громко
- Попробуйте записать 15-20 секунд

**Ошибка скачивания:**
- Проверьте интернет соединение
- Убедитесь что yt-dlp установлен: `pip install yt-dlp`

