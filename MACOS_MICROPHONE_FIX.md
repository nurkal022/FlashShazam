# Исправление проблемы с микрофоном на macOS 🎤

## Проблема
Запись идет, но файл содержит только тишину (уровень звука 0.0%).

## Решение

### 1. Разрешите доступ к микрофону для Terminal/Python

**macOS Ventura и новее:**
1. Откройте **Системные настройки** (System Settings)
2. Перейдите в **Конфиденциальность и безопасность** (Privacy & Security)
3. Выберите **Микрофон** (Microphone)
4. Найдите и включите переключатель для:
   - **Terminal** (если запускаете через терминал)
   - **Python** (если запускаете напрямую)
   - **iTerm** (если используете iTerm2)

**macOS Monterey и старше:**
1. Откройте **Системные настройки** (System Preferences)
2. Перейдите в **Безопасность и конфиденциальность** (Security & Privacy)
3. Вкладка **Конфиденциальность** (Privacy)
4. Выберите **Микрофон** (Microphone)
5. Установите галочку для Terminal/Python

### 2. Перезапустите терминал
После изменения настроек:
```bash
# Закройте и откройте терминал заново
# Или перезапустите сессию
```

### 3. Проверьте уровень громкости
1. Системные настройки -> Звук (Sound)
2. Убедитесь что уровень входного сигнала не на минимуме
3. Проверьте устройство ввода

### 4. Проверьте устройство
```bash
# Запустите тест
python test_microphone.py

# Проверьте записанный файл
python check_recording.py recordings/recording_*.wav
```

### 5. Альтернатива: Используйте браузерную запись
Если серверная запись не работает, система автоматически переключится на браузерную запись через Web Audio API.

## Проверка прав доступа

Запустите в терминале:
```bash
# Проверит доступ к микрофону
python -c "import pyaudio; p = pyaudio.PyAudio(); print('Доступ к микрофону:', p.get_default_input_device_info())"
```

Если видите ошибку о доступе - нужно разрешить права в системных настройках.

## Альтернативные устройства

Если встроенный микрофон не работает, попробуйте внешнее устройство:

```bash
# Список устройств
python test_microphone.py

# Используйте устройство по индексу в app.py или через API
curl -X POST http://localhost:5001/api/process \
  -H "Content-Type: application/json" \
  -d '{"duration": 15, "device_index": 2}'
```

